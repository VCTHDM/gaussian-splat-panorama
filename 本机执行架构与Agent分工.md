# 视频 → 高斯场景 → 地形模型：最短执行方案

更新：2026-09-10。目标是尽快从现有视频做出能打开、能看的局部场景和地形参考网格。环境锁定见 `docs/environment.md`。

## 就走这一条路

**复用已抽好的 64 帧 → 全景转透视图 → COLMAP 求相机和稀疏点 → PGSR 训练 → 打开高斯结果、导出网格。**

先做最短的 47.6 秒视频 `全景素材/f640ef4cc3c8cbda0df51f7eb2d79368.mp4`。这段实际为 1280×640，不再按旧文档里的“20 分钟 8K 全场工程”安排工作。

1. **接上现有准备。** 帧在 `jobs/shortest-pilot/sfm-prep/`，复用 `scripts/sfm/` 和 `GS-Ubuntu2204`。直接运行需要的步骤，缺什么依赖补什么，不重复整机体检或重装环境。
2. **跑出相机和点云。** 沿用现有 384 像素、12 视向固定 Rig 的展开方案和 COLMAP/pycolmap 路线。同一全景展开的视图共享相机中心；这是输入处理需要，不另设验证阶段。复用已有遮罩与帧映射。
3. **接着训练。** SfM 有可用的相机和点云就接 PGSR，补齐所需 PyTorch/CUDA 依赖。先用低分辨率跑出可看的版本，能保存就先保存并展示，再继续改善。显存不足再降分辨率或点数，不预先改造训练框架。
4. **交出实物。** 高斯文件配一个本地查看入口；随后用 PGSR 现有导出流程产出地形/场地网格，在已有 Blender 或 Rhino 中打开。先出的点云或高斯可以立即展示，网格仍要接着完成。

高斯用于自由视角观看，网格用于建模参考，两者分别交付。没有已知尺寸时先用相对尺度；不把测量精度、控制点或坐标标定设为出第一版的前提。

## 谁来做

- Codex 直接推进任务、解决阻塞、把结果展示给用户，普通修改和运行不需要再设一个审查角色。
- 需要 Grok 写脚本时，沿用本机 `grok-4.6-build` 和现有调用入口，给一个具体任务即可。Grok 是可用执行工具，不是每一步必经环节。
- 重建和训练由本地程序计算；同一时间只跑一个重任务。无需新增多 Agent 岗位、任务队列、资源锁框架或状态管理系统。

## 以结果推进

运行报错就修当前错误；结果能打开就展示。必要检查随实际运行完成，不单开探针、验收任务或报告工程，不以注册率 90% 等固定数字阻止查看和继续可用部分。

第一版取消全量分块、双块试验、LOD、独立精度报告、统一 schema、专门基准测试、恢复框架和预先改造缓存/深度融合。以后遇到实际瓶颈再解决需要的那一项。

每次交接只需说明：产物路径、当前卡在哪里、下一条操作。保留程序自己的错误日志即可。出错时保留已有成果，针对原因修复；不要不断重跑同一失败配置，也不要把点云或未完成训练写成最终模型。

## 现有状态与边界

截至 2026-09-10：47 秒局部高斯和地形网格已出。实际步骤写在 `docs/executed-pipeline.md`，待 GPT 验收后再升正式管线。后续状态只更新 `TODO与进度.md`。

保留原视频，不上传素材，不删除既有成果，不自动重启。已有安装授权继续有效；若有新的自动审批拒绝，按拒绝处理，不绕过。

本方案替代旧的多 Agent 和逐阶段验收流程。旧任务书、调用指南及历史报告只用于找命令、参数和已有文件，不再引入旧的前置关卡。47 秒候选管线见 `docs/executed-pipeline.md`。
