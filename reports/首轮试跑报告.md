# 最短片首轮试跑（轻量汇总）

编制时间取本机时钟，见文末。本文件只汇总已有产物，不声称重建完成。

## 输入

- 最短片（按 `ffprobe` duration 升序，不是文件大小）：`全景素材/f640ef4cc3c8cbda0df51f7eb2d79368.mp4`
- 时长 **47.594944** 秒，画面 **1280×640**，解码 **476** 帧
- PTS：`ffprobe -show_frames json`，476 行，首 0.0、末 47.494967，负/零间隔 0，单调通过（`jobs/shortest-pilot/sfm-prep/run_meta.json`）
- 0.75s 窗口 **64** 个，各取最佳帧 64 张；原视频未上传、未改

## 相机与投影

- 用户说相机为「美碳v3」。兄弟文件 `video_20260906_180959.mp4`、`video_20260906_181540.mp4` 元数据为 mentech / PanoX V3（3840×1920），作为相机来源线索。
- 最短片本身无 spherical 标签；视觉检查支持 **equirectangular 试跑假设**，不是标定证明
- 相机内参、相对坐标与单位：**未知**。不得把最短片当成 8K 原片

## 未做

- SfM / COLMAP：**未运行**
- 四向512像素透视图仅是诊断预览；原方案12个重叠视向固定Rig、位姿注册与稀疏模型仍待实施。
- PGSR：**未克隆、未编译、未训练**
- WSL/Ubuntu：MSI 2.7.13 **已安装**（msiexec exit 0，`wsl --version` 成功）。`WslService` 启动后停止（SCM 7023 / Win32 1058）。HKLM IWslSupport 接口键不存在。Ubuntu **未导入**（目标目录 0 文件）。上次 `distro_install exit -1` 是 Codex 结束卡住的导入进程（client 26368 / 父 17152），不是自然错误也不是新审批拒绝。06 Grok 因轮次上限退出，不是安全拒绝。未做 uname / nvidia-smi。见 `reports/wsl-service-diagnosis.json`。

## A0 最终验收

2026-09-09 20:25:52（本机 UTC+8）完成离线文件复核：

- WSL 2.7.13 MSI：258,985,984 字节，完整 SHA256 与固定官方值一致；Authenticode 为 Valid，签名者 Microsoft Corporation。
- Ubuntu 22.04.5：360,684,292 字节，最终目标文件 SHA256 与固定官方值一致。分段合并结果通过，原有91MB部分文件已保留为派生备份。
- Grok 调用包装器25项本地测试通过；真实CMD探针退出0，正常停止并精确返回 `GROK_WRAPPER_OK`。
- 最短原视频 SHA256 再次核对一致。现阶段验收对象为输入预处理与调用路由，尚无三维模型。
- 两次长Grok执行未正常完成，分别涉及自动权限检查/取消，以及轮次上限。其退出码不能当成业务成功；A0依据实际产物验收并监督已生成的本地续传脚本完成离线准备。

当前状态：**needs_user_choice_os_component_enable**（`reports/wsl-install-handoff.json`）。MSI/Ubuntu 的用户批准仍然有效，不必重复批准同一 msiexec。新阻塞是本机已暂存、包内已禁用的 `Microsoft-Windows-Subsystem-Linux`；`Get-FeatureInfo` 仍报 0x800f080c。本会话自动审批拒绝了 DISM Enable-Feature。恢复路线 A/B 见 `reports/wsl-service-recovery-plan.md`。不要仅凭 1058 重启。服务可用前不导入发行版、不推进 SfM。

本机时间：见 `reports/stage02-handoff.json` 的 `created_local`。

## A0 安装后复核（2026-09-09）

MSI 安装与版本查询通过；Grok 07 诊断会话退出 0 / end_turn，诊断完成不代表 WSL 可用。包装器回归 25/25，安装脚本解析无错误。指定本机包的 Get-FeatureInfo 已独立成功（exit 0，Disabled，Restart Required Possible），只有未指定包的普通查询报未知功能。下一步仅申请一次本地 Enable-Feature，审阅范围以 `reports/WSL组件启用方案.md` 为准；无 Add-Package/IgnoreCheck 后备，未执行系统组件启用。

## 最新状态：组件已启用，等待 Windows 重启

用户最新授权已记录，未关闭或绕过自动审批。Grok 执行一次已审阅 DISM Enable-Feature 后，Codex 独立查询成功（exit 0）：Microsoft-Windows-Subsystem-Linux = Enabled。CBS 会话 31277147_3811460562 最终 S_OK，明确 Reboot required yes，CBSRebootPending 已存在。没有自动重启，IWslSupport 此时仍未出现，Ubuntu 尚未导入。

DISM 宿主/客户端在退出码写盘前消失，因此实际客户端退出码未知，不能捏造 3010，也不能重复启用。以上完成结论来自系统状态查询及 CBS 日志。报告：reports/wsl-feature-enable-result.json、reports/a0-wsl-feature-review.json。当前阻塞是重启，不是权限确认。重启后从 .grok-tasks/09-after-reboot-ubuntu.md 继续，环境通过后运行已准备的03最短片SfM任务。历史待批准/Disabled条目仅表示此前状态。
