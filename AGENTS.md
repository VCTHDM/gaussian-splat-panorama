# 项目工作约定：先出模型

用户当前指令优先。当前目标：用现有最短视频做出可查看的高斯场景和地形参考网格。

- 执行路线见 `本机执行架构与Agent分工.md`；当前进度只记在 `TODO与进度.md`；版本与路径锁在 `docs/environment.md`；**已跑通的候选管线**在 `docs/executed-pipeline.md`（待 GPT 验收，尚未正式）。续跑先读这几份，不通读历史报告。
- 复用已有 64 帧、`scripts/sfm/`、`GS-Ubuntu2204`。WSL 和 GPU 驱动接口已经可用，不重复重启、安装 MSI、启用组件或下载 Ubuntu。SfM 环境 `/opt/gs/env/gs-sfm` 与 PGSR 环境 `/opt/gs/env/pgsr`（torch 2.4.1+cu124）均已可用，不要重装。
- 用户启动执行后，直接推进“全景展开 → SfM → PGSR → 查看高斯和导出网格”。缺依赖就补，报错就修，可用结果先展示；不在阶段之间等待用户或另一 Agent 验收。
- Codex 可以直接修改和运行。Grok `grok-4.6-build` 按需使用，不强制转交；不新增 Agent 岗位、队列、状态机或验收框架。
- Grok 调用复用 `scripts/invoke-grok.cmd`，默认新会话，不用曾卡住的 resume。仅需要调用细节时查 `docs/Grok调用指南.md` 和 `docs/Grok-CMD补充.md`。
- 旧 `.grok-tasks/03-shortest-sfm.md` 及其他历史文档仅供参数/命令参考；其中强制先验收、只准 SfM 不准训练、复杂报告和旧职责分工已取消。续跑以本文件和精简方案为准，不直接原样执行旧任务书。
- 只做实际运行所需检查：命令出错、相机/图片不匹配、结果打不开就处理。不专门写验证脚本、指标报告或性能测试；不把固定注册率、精度或全量规划作为第一版前提。
- 一次只跑一个重计算。保留程序日志和已有产物，失败修当前问题；进度只写产物位置、阻塞、下一步，不虚报完成。
- 保留 `全景素材/` 原视频，不上传或替换，不删除用户数据，不远程 push/发布，不改凭据/驱动，不自动重启。
- 已批准安装范围见 `configs/install-authorization.md`，不为同一操作重复询问；新的自动审批拒绝必须等用户明确批准，不修改为 bypassPermissions 或换工具规避。
- 47 秒完整高斯（30,000 次）和地形网格已出，见 `jobs/shortest-pilot/results/`。demo 保留。未要求前不要重训或改走更长视频。一次只跑一个重任务。
